<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uw.wensix.leetcodelegend">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LLApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeetcodeLegend">
        <activity android:name=".activity.EditProblemActivity"/>
        <activity android:name=".activity.ProblemDetailActivity" />
        <activity android:name=".activity.PastProblemActivity" />
        <activity android:name=".activity.VisualActivity"/>
        <activity android:name=".activity.PerformanceActivity" />
        <activity android:name=".activity.TimerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>